# 类别与定位最新进度（2026-09-07 11:19 +08:00）

**新结论：N/C 三 seed 均完成 E200 独立评估，C−N mAP50–95 为 +0.266655±0.144373 pp，三个 seed 同方向；定位 L 尚未达到几何准入条件，没有 CL/CGT 长训结果。** SD 为样本 SD。此记录更新此前单 seed 和 same-modal 排队状态。

## 目的与设置

只读核对 94 原始训练进度、独立评估和资源状态。本次没有启动 GPU 任务。DroneVehicle：E200、640、batch32、nbs64、workers4、SGD、lr0/lrf=0.01、AMP；固定 last/EMA，在 1469 张开发验证图像独立评估，未使用测试集。N 是同代码 weight0，C 为原 OEv1，教师与 RGB 参考固定，学生 seed 为 0/42/123。

## 完成结果

| seed | N mAP50–95 (%) | C mAP50–95 (%) | C−N (pp) | AP75 差 (pp) |
|---|---:|---:|---:|---:|
| 0 | 54.346185 | 54.772186 | +0.426001 | +0.233624 |
| 42 | 54.513608 | 54.658162 | +0.144554 | −0.327243 |
| 123 | 54.407436 | 54.636847 | +0.229411 | +0.525296 |

N 均值为 54.422410±0.084710，C 为 54.689065±0.072770。配对 AP50 差 +0.087126±0.256517 pp（1/3 正），AP75 差 +0.143892±0.433295 pp（2/3 正）；报告 Recall 差 −1.172702±1.239639 pp（3/3 负）。不同模型的报告 Recall 可能对应各自评价操作点，不能据此直接断言固定阈值下新增漏检；需统一操作点及对象级损伤/修复分析。

结论限定：支持当前开发协议下小幅、三个 seed 同方向的 mAP 改善，不自动证明统计显著性、跨模态内容归因或负迁移消除。历史约 +1 pp 不作为正式配对增益。六个端点均通过现有 v2 分析器的完整性检查；四臂归因仍未齐备。

## 在跑实验

| 实验 | seed | 当前轮 / 200 | 独立评估 |
|---|---:|---:|---|
| C-random | 0 | 163（完成162） | 无 |
| C-random | 42 | 172（完成171） | 无 |
| C-random | 123 | 159（完成158） | 无 |
| C-shuffled | 42 | 31 | 无 |
| C-same-modal | 42 | 15 | 无 |

random 最近轮时估计剩余训练约 2.1–2.9 小时，不含评估排队且随共享负载变化。两个内容归因实验仍在早期，不使用上述 ETA。

C-random 随机改变对象选择；C-shuffled 保留 paired 选择与剂量，只替换错配 IR 内容，是实例与空间对应同时破坏的压力对照；C-same-modal 使用独立 RGB N0 教师及原 RGB seed42 参考，对象、门与目标全部来自 RGB。后二者是 C-only，λL=0，不是定位实验。OS-SSL、VEDAI 保持暂停；完整 FGD/LD 未进入本轮长训。

## 两个分支的实际身份

C 保持原 OEv1：在 P3/P4 蒸馏对象前景相对背景的类别证据，教师质量选择，T=2、λC=0.1。其资格来自冻结 RGB 参考的粗候选，不代表当前学生定位已经准确。

L 算子、同掩码 GT 控制及诊断代码已实现：通过独立双模态 GT 确定对象，在冻结参考预选的唯一合法 anchor 上，类别/置信度合格且教师定位占优时迁移同 stride/bin 的 DFL 分布。CGT 保留相同选择和剂量，仅换为温度一致的 GT 两 bin 目标，用于区分教师内容与重复 GT 监督。

尚未完成 L 的实际梯度校准、正式 L/GT 真实训练 canary，未启动 CL42/CGT42 E200。本次远端查询未发现 calibration_receipt.json。

未核验几何限制的静态 raw-anchor 机会诊断（不是实际增强训练覆盖率）：

| 数据子集 | 图像 | RGB GT | L 条件通过 | 占 RGB GT |
|---|---:|---:|---:|---:|
| Drone train | 2048 | 31931 | 626 | 1.96% |
| Drone dev | 200 | 3084 | 112 | 3.63% |
| LLVIP train | 2048 | 5592 | 211 | 3.77% |
| LLVIP dev | 200 | 643 | 131 | 20.37% |

这些结果说明存在候选监督机会，但不能直接授权同 anchor 空间蒸馏。冻结抽样 300 对，已看 48 对；仅 LLVIP050001 局部 6 点独立接受，该覆盖区域未涵盖其两个完整 GT，加入当前已核验几何合同后 D2 对象数为 0。这是证据覆盖不足，不是“整个数据集未配准”或“定位蒸馏无效”的结论。完整物理点审计没有完成。D2 是参考选定 anchor 的机会，不等于对象所有候选都定位差，也不等于当前学生状态。

## 资源

11:19：5 个活动 lease，占 3 张物理 GPU（2/4/5），每卡最多 2 个项目任务，项目 RSS 约 141.33 GiB。当前服务器没有完全空卡；按常规三卡约束运行，不援引旧快照的四卡放宽条件。

## 产物与复核入口

- 原始 154 文件快照：`../2026-09-07_audit_RGBIR实施起点/snapshots/2026-09-07T111931.041292_0800/`。
- 本目录 `results_analysis.json`、`endpoint_table.csv`、`progress_eta.json`、`resource_summary.json` 为该快照派生产物副本；`content_status_111931.json` 为内容控制状态原始查询。
- `collect_readonly.py`、`build_audit.py` 为本次使用脚本副本；解析器与原始输入身份由原快照保留。
- 定位诊断：[INDEPENDENT_RESULTS.md](../2026-09-07_probe_TaskConditional机会诊断/INDEPENDENT_RESULTS.md)。
- 94 内容控制路径：`/mnt/dataset/yudongfang/projects/RGBT_campaign/runs/rgbir_task_conditional_c_attribution_20260907/`；大权重未下载。

下一步是收齐 random 与内容归因端点，同时继续补 L 几何覆盖；只有几何、非零监督、校准与 canary 均通过才进入 CL/CGT 长训。不会因当前 AP 改门控或调 λ。本次新结果已本地落盘，尚未同步 GitHub。
