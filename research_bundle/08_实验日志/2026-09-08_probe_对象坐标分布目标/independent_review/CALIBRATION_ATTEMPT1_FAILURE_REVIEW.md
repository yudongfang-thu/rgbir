# Calibration attempt1 failure: independent review

**The recorded calibration failure is real and was stopped by its internal frozen resource gate. A retained final loss graph is a verified source defect and a plausible contributor; its share of the actual VRAM increase remains to be measured by the fixed v2 eight-batch run.**

The original `training_evidence_1703/calibration/llvip/failure.json` reports `OBJECT_DFL_CALIBRATION_FAILED` at the resource check. Batch1 has NVML 5454 MiB, allocated 4389.493 MiB, reserved 4954 MiB, giving the prescribed 5888 MiB reservation after margin. Batch2 has NVML 8506 MiB, allocated 7376.646 MiB, reserved 8006 MiB, giving 8960 MiB after margin, above the unchanged 8192 MiB ceiling. RSS after margin is 19456 MiB on both rows, below 32768 MiB. The independent dispatcher record has no monitor errors and reports 12077 MiB minimum whole-card free memory. This was not evidence that the other-user 2 GiB reserve was breached.

The queue's only launched stage was calibration; it terminated as `DATASET_STOPPED_ON_TECHNICAL_FAILURE`, without a calibration coefficient receipt or any new canary/training endpoint. The failed attempt must not supply a lambda or be interpreted as a KD result. No resource ceiling, batch selection, gate, coefficient rule or retry identity is changed by this finding.

The executed calibrator loops over `for arm,(loss,stats) in losses.items()` with `retain_graph=True`. Its end-of-batch deletion removes `losses` but omits the final `loss` loop variable. That scalar still owns the GT/student autograd graph at the following forward. Deleting `native_items` explicitly additionally avoids dependence on whether an inherited native diagnostic is detached. The proposed v2 diff removes those references and records actual allocation at batch start, before cleanup, and after cleanup; it does not call `empty_cache`.

The reviewer's bounded CPU test tracks a custom autograd context that owns saved tensors. Old cleanup keeps the context alive at the next forward; explicit final-loss deletion releases it, and the two batches' gradient norms remain exactly equal. Evidence: `CALIBRATION_LIFETIME_INDEPENDENT_CPU_attempt2.json`. The first reviewer fixture tracked the Python Tensor wrapper, which does not establish underlying C++ saved-storage lifetime; that inconclusive/failed fixture is preserved as `CALIBRATION_LIFETIME_INDEPENDENT_CPU.json` and is not presented as a successful mechanism test.

Current source correction is limited to graph lifetime and observability. The earlier release_v1/source READY is retained as history and does not authorize changed v2 bytes. A separate v2 source review, affected CPU checks, exact deployment identity and the same fixed eight actual batches are required. Only those actual resource records can establish whether the production VRAM growth is resolved.
