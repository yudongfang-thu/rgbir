# Independent review trace

The parent supplied original artifact locations and a bounded review task: inspect the annotation-defined probability transport operator; write only independent_review; use direct byte/path identity without new hashes; issue source READY before CPU cache execution; then independently reconstruct all 80 GT and the two fixed teacher-source paths. No GPU or new forward was authorized to the reviewer. Required output is the complete EXPERIMENT_AUDIT schema with explicit target-interface limits.

Reviewer: `/root/object_transport_review`. Executor: `/root/ap_error`. Model settings were inherited; exact observable model identity was not exposed. No cross-model claim is made.

Reviewer read AGENTS.md, experiment-audit/SKILL.md, root/index/latest conclusions, prior NEXT_TARGET_INTERFACE_PLAN.md, and four original cache inputs. The skill hash default was superseded by the user's explicit no-new-hash scope. The parent clarified that the frozen CPU source probability is FP64 T=1 stable softmax of actual logits, with saved FP32 probabilities retained and disclosed separately. That clarification was passed to the executor before final source acceptance.

Independent source response: 37 mathematical checks passed, full-bin hat-basis probabilities and mapped distances exact against the operator. Source correction requested: add dfl_contract.json to the declared and verified inputs; executor did so before execution. Initial READY and source snapshot were saved. Executor then changed only two empty-summary max fallbacks from zero to null; independent direct diff accepted that change, saved a v2 source snapshot, and retained the earlier review as history.

Executor reported actual output_attempt1 completed. Reviewer then ran audit_cached.py against the original accepted cache, never importing the producer operator. All 160 result rows and 628 available side vectors passed. Parsed findings are the actual machine outputs in source_small_truth_result.json, recomputation_summary.json and recomputation_rows.json. Final direct-byte comparisons and complete findings are in EXPERIMENT_AUDIT.json. Final reviewer response is EXPERIMENT_AUDIT.md.
