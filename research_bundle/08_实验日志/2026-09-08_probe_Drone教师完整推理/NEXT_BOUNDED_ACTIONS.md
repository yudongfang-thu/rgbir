# 完整双数据集证据后的下一项有界执行

**下一工作窗口只解决“对象优势如何成为学生实际可学习且不同于GT的信号”，优先LLVIP；不再重复完整dev、首32图、历史L2桥接或本次cross-GT。** 本文件在本轮结果后制定，是下一阶段的前置协议，不冒称此前outcome-blind结论。

## 输入与第一步

先复用`2026-09-08_probe_训练侧定位覆盖`首32图的11个定位机会、1个反向对象、其余对象，以及`2026-09-08_probe_快速方向筛选`既有8批L2校准原记录。用稳定图像/GT/anchor身份整理：原参考学习anchor、原生检测匹配框是否同位置、四边学生/教师/GT目标、实际门的退出位置。原.70门及旧selected保持历史身份，未执行教师后门保持null，不用代理补成“通过”。

已有8批记录足以回答的统计全部CPU完成；不要为凑总体样本再取128图。先明确缺失字段清单，而非重跑完整见证。下一个报告应直接给出三项结论：

1. 真实定位机会能否落到原训练学习anchor；原生检测框与选择anchor不同是否解释已有覆盖/剂量。
2. 教师内容与同anchor GT目标到底差在哪：分别保留坐标目标差、输出导数、已有共享参数梯度证据；不能把坐标导数余弦当参数余弦。DFL或局部特征的带宽只在有实际输出时讨论，不从post-NMS框假推。
3. 若传的主要还是重复GT坐标，当前L2不继续只靠扩大覆盖救活；只有发现可连接且超出该GT控制的具体内容，才冻结下一版短程方法。

## 确实缺输出时的唯一新增探针

仅允许补原自然流首批32图、成熟S/R/T、0 optimizer update的必要raw输出，保留原增强/AMP/BN合同与流身份；已存在字段不重复当新结论。对象选择不使用dev名单，不放松原物理配准合同。先复核源码与小真值，再按现有唯一lease、screen、资源规则测量和执行；该探针不顺带训练。若在当前缓存中已能闭合，则不启动GPU。

这项工作以一个短窗口为界：目标30分钟内得到有/无具体教师内容的决定；资源排队、接口错误或缓存不足须如实报告，不能把目标时长当完成承诺。诊断完成就落盘并作去留决定，不无限追加新的机会计数。

## 如何进入下一次短训

新短训必须针对上面识别的一个具体内容差异，另写单一新假设、自然训练对象集、真实学习位置、目标、固定剂量及同掩码GT控制。复用已有分钟级框架前必须解释LLVIP成熟N的0.707pp漂移，不能简单把再一次FT3小差当方向淘汰结论。先行预算最多N/教师内容/同掩码GT三个短臂，启动前冻结，不现场扫λ、阈值或子集；当前文件不提前宣布它们已准入。

Drone的本次cross-GT已经给出高标签一致性对象的正向证据及低一致性的反转风险。保留这份完整对象表作为第二优先级，不再补相同推理；训练方法设计不得把GT框一致当物理点配准、不得在未验证支撑/语义时跨anchor直接传DFL。

所有旧E200由原协调器继续完成与独立评估。本轮方法推进不占用其完成责任，不新增E200。阶段证据继续上传既有GitHub分支，只有新结果、失败或方向决定才通知用户。
