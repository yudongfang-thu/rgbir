ACCEPTED

限定接受 `cross_gt_attempt1` 的固定原生 IoU .5 匹配框对两份 GT 的比较。6 项合成真值独立通过；完整 2819 对对象、11276 个标量 IoU、原 GT/预测 ID/框/置信度、.75 匹配身份、两层分母和全部计数独立闭合，IoU 最大差为 0。源副本字节一致，原输出 PENDING 状态保留。

固定 cohort 由 score > .25 时双方 .5 正确、只有一侧在独立 .75 匹配正确定义，人数为 T 侧 1869、N 侧 950。主框始终来自各自 .5 原匹配：T 侧 cohort 有 2 个 T 的 .75 匹配换框；反向 cohort 有 1 个 N 的 .75 匹配换框，均只另列记录，未替换主统计框。因此不能把 cohort 的 .75 正确身份直接当成每一个固定 .5 框都达到 .75。

| 固定 cohort / 原标签 IoU 层 | 分母 | T 固定框→RGB GT ≥ .75 | 同一 RGB GT 上 T−N > 0 | > .05 | < 0 | < −.05 |
|---|---:|---:|---:|---:|---:|---:|
| T 侧 .75 独有，全部 | 1869 | 1639 | 1693 | 1555 | 176 | 120 |
| T 侧，标签 [.5,.8) | 246 | 41 | 79 | 39 | 167 | 118 |
| T 侧，标签 [.8,1] | 1623 | 1598 | 1614 | 1516 | 9 | 2 |
| N 侧 .75 独有，全部 | 950 | 113 | 32 | 10 | 918 | 819 |

原始两组及各标签层的图数、覆盖 .5/.75、正负/零差、own 正优势变 cross 非正优势和 same/changed/unmatched 计数均已核对。上述差值均为固定两框在**同一 RGB GT**上的 IoU 差；表中分母保持原 1869/950 或预先固定的标签层，未丢掉劣势对象，也未以更佳的 .75 框替换。

可支持的结论是：在共同 native 数值画布直接比较时，多数该组固定教师框的定位优势仍可在 RGB 标签下观察到；较低标签 IoU 层出现较多反向比较，不能据 IR own-GT 优势全量替换 RGB 目标。反向 cohort 保留了明显的 RGB 框优势。当前证据不能把每个反转都因果归为标签错位，也不证明未反转对象已物理对齐。

这里没有注册变换、几何校准、候选重选、重新匹配或 NMS；画布都是 544×672，原图 512×640，未隐式缩放或 clamp。它是近似恒等坐标下的交叉标签读出，不能提升为 L1 几何准入、实际 L2 选择、可蒸馏收益或新学生收益，不授权放门或更改系数。

独立回执：`CROSS_GT_CPU_REPLAY.json`、`CROSS_GT_ACTUAL_RECEIPT.json`；复算脚本 `review_cross_gt.py`。无 GPU、模型 forward、权重加载、新 hash 或 AP 计算。
