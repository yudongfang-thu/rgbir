# LLVIP 050001 静态结构点独立复核

2026-09-07，root实际查看 `llvip_050001_primary_points.png`，并分别以原分辨率查看 `llvip_050001_RGB_raw.jpg`、`llvip_050001_IR_raw.jpg`（1280×1024）。未查看检测框/GT。

六点的结构身份可以接受：a/b/c分别是同一三个井盖的椭圆中心；d/e/f分别是同一三个短路桩顶面的中心。与上一批不同，这些点不依赖车辆灯光边缘或移动行人轮廓。主标坐标与原图观察相容；采用主标声明的额外3个原图像素不确定度，不能解释为精确或亚像素标定。

复核决策：接受这6点作为 **050001这一帧** 的经验对应证据，允许依照固定容差计算对象掩码；仅限两侧点凸包交集覆盖的区域。不能直接授权整个prefix，更不能授权全LLVIP。基于主标数值，在640输入、无额外缩放时，含不确定度P95约3.247像素、max约3.303像素，因此P3不通过；P4还需目标短边满足0.1×短边≥3.247像素、整个目标落入已覆盖区域。增强后重新传递误差并判断，不能固定沿用无增强结果。

这份接纳只核实近似几何线索，不证明该帧存在教师质量更高的实际anchor；后者必须由D2独立计算。
