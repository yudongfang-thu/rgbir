# 完整 dev 读出后的下一项有界工作

**先完成可直接复用的 CPU 检查，再补缺少的推理；不启动新训练矩阵。** 本文件在本轮完整 dev 结果后冻结，用于下一次主动执行，明确属于后续工作而非当前完成结果。

## LLVIP：检查定位对象在训练侧是否存在

1. 先复用上一条 `witness_evidence_1315_final/probe/witness_frames.jsonl` 与 GT/对象映射，对原32训练图的已保存 score>0.25 post-NMS输出补 IoU0.75 原生源码 CPU匹配；不用GPU、不重复首批前向。与该批IoU0.5状态、原C的base/eligible/selected按既有稳定ID交叉，报告双方粗检出/仅T精确与反向对象的数、图像数及选中覆盖。
2. 单批只说明该批。需要扩展训练覆盖时，范围固定为既有LLVIP自然训练流的前四个batch，共128次图像出现；保留原数据顺序/增强/随机性/BN与AMP，成熟S/R/T、0更新，不挑图或过采样。已有首批缓存不再作为新结果重复计数，新增第2–4批需可靠衔接原流与ID；无法验证则先修接口。原生匹配始终独立按.5/.75，不放宽旧L1门。
3. 只有实际需要新前向的后三批走原global lease、原资源纪律与screen，保留同一路径短测及峰值回执；本轮已完成的两个GPU探针不重跑。先保存逐对象结果，不现场扫阈值/λ、不顺带训练。
4. 这里的机会只是训练侧候选，不自动满足原L1物理几何合同。若机会充分但原L门未覆盖，报告具体过滤环节；若成熟模型下机会稀少，明确记录，不能仅据dev机会多反复做成熟模型微调。

## Drone：补全教师输出的证据缺口

先只读核对94原teacher运行/评估产物及项目索引，寻找具有完整dev、模型/split/GT/坐标/后处理合同的IR42缓存，不凭文件名full或只读summary认定存在。已有RGB N42完整输出保留，无需重复推理。

若确实没有可用IR输出，下一项GPU任务只补一次Drone IR42完整dev推理（目标1469图，执行前核对双模态清单），沿用现有接受的native capture入口和RGB缓存的评价设置。先以固定首批验证IR独立标签完整、类序、canvas、空预测保留、资源，再经唯一lease执行；不训练、不加载test、不下载权重。使用原IR标签，与RGB GT的对象关联单独按既有同类一对一IoU≥0.5规则验收，未匹配GT分别报告，不把标签配对当物理配准。

完整N/T输出可用后，复用本轮接受的置信度/IoU四组CPU分析器，补Drone真实类别与定位互补；类数从单类扩到五类须先通过对应真值及独立审核。首次只比较固定模型，不增三seed教师或训练矩阵。

## 向短训推进的界限

新的定位或响应短训需先写清：针对哪类真实对象、自然训练中有多少、学生学习位置如何确定、教师内容与GT控制有何差别。已有九次短训不重复，原E200任务继续由其协调器负责。没有这些条件时完成诊断/接口修复即可，不用扩大模型、换loss或拉长E200替代问题定位。
