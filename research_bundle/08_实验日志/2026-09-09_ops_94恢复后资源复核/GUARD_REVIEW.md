# 94 统一资源 guard 只读复核

**guard 动态枚举实际 GPU，没有固定旧卡号造成的调度阻碍；当前实现上限为4，但第四卡所需“分配后仍余至少两张完全空卡”没有自动执行。此次只做状态/可行性答复，启动方继续保持最多3卡，不占满现有4张空卡。**

## 来源与时间

2026-09-09 **15:07:47.951599 +08:00** 只读取得共享 lease：`leases={}`。原始文本摘要与源码行摘录见 [GUARD_REVIEW.json](GUARD_REVIEW.json)。

- 实际源码：`94:/mnt/dataset/yudongfang/projects/SpaceNet6_OTD_official_reproduction/tools/project_resource_guard.py`，803行。
- 实际共享 lease：`94:/mnt/dataset/yudongfang/projects/SpaceNet6_OTD_official_reproduction/runs/.project_resource_leases.json`。
- 此次只读取源码和 JSON，没有导入或调用 guard 的 inspect/acquire；inspect 本身会刷新、写回 lease，故没有使用。未启动 CUDA、nvidia-smi、训练或后端检查，未改共享源码/lease，未计算新摘要。

## 源码证据

| 检查项 | 实际行号与内容 | 含义 |
|---|---|---|
| GPU枚举 | 93–95行查询 nvidia-smi 的 index/free/used/total；722行取得当前快照 | 使用当时实际可见 GPU |
| 默认候选 | 694行：未显式给候选时用 `sorted(rows)` | 不写死0–7，也不强制旧索引 |
| 已失效候选 | 423–425行检查候选是否存在于当前快照 | 显式旧卡号失效会报告，不静默映射 |
| 真正执行上限 | 29行 `MAX_ACTIVE_GPUS = 4`；483行按该常量筛选组合 | 当前代码不是固定3卡上限 |
| 陈旧元数据 | lease中 `policy.max_active_gpus=3`；487行拒绝提示仍写3 | 元数据/提示与实际常量不一致，不应据提示误判实际卡数 |
| 第四卡条件 | 450–498行只筛选项目卡数、单卡进程/显存/安全余量、正式训练数量，再按headroom选卡 | 未统计分配后完全空卡数；29行的“余≥2空卡”目前只是注释 |
| 调用方设备映射 | 743行由获准lease设置 `CUDA_VISIBLE_DEVICES` | 已获准物理卡动态映射给新进程 |

本表的“未自动检查”是对当前 acquire 条件分支的源码判断；没有通过申请第四卡来试验，也未修改它。

## 当前约束如何理解

主执行任务15:05快照显示94仍有8×4090：0–3为空卡，各24082 MiB free；4–7为共享忙卡，各11584 MiB free。该硬件快照由主任务取得，本子任务没有重复GPU查询。

按工作区 AGENTS.md，默认本项目最多使用3张卡。放宽到4张时，要求服务器较空闲，并且**分配完成后仍有≥2张完全空卡**。因此，直接占满现有4张空卡不符合放宽条件；这不等于禁止任何共享忙卡情况下的总4卡方案。若例如只用其中2张空卡、另用2张忙卡，仍留2张空卡，并通过共享卡峰值/每卡剩余≥2GB及其他资源要求，原则上可以满足第四卡条件。此例只是条款解释，不是本轮调度或准入。

本轮决定仍为：最多3卡、先按现有实测峰值与实际候选分配；不因为代码常量为4就自动使用第四卡。不恢复旧实验，不将本次只读检查视为训练启动授权。共享README/索引由主任务统一登记。
