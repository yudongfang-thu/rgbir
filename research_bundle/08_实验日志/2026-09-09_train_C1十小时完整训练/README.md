# C1加速版十小时完整训练（2026-09-09）

**C1加速版seed42于16:46:12在94的GPU0启动完整E200；7项运行控制测试及三个seed的原配置/准入预检通过。尚无新完整AP。训练、保存与独立dev评价共用单run十小时预算，seed0/123在首5轮工程耗时门通过后自动接续。**

## 目的与方法

得到当前缺失的C1完整末轮结果：检验保持OEv1对象选择时，将压缩类别证据换成P3/P4全部类别相对raw logits，是否优于匹配N和原C0。C1为独立分类分支，不包含定位分支。先前短筛不能代替这个完整端点。

## 固定设置

| 项目 | 实际设置 |
|---|---|
| 数据 | Drone train17990/dev1469；封存test不访问 |
| 模型/起点 | YOLO11n，从原通用预训练重新初始化，冻结既有IR教师与RGB参考 |
| 方法 | C1 paired，P3/P4前景相对背景全类别Bernoulli KL，温度2，非目标类系数0.25 |
| 损失系数 | 分类0.09227393550836771，定位0 |
| 训练 | E200，640，B32/nbs64，workers4，原SGD、学习率、增强、AMP与loader随机流 |
| 评价 | E200 last/EMA，原独立评价器，完整1469张dev，不挑best |
| 对照 | 匹配N42 mAP54.513608%、C0_42 54.658162%；正式结论仍需三seed |
| 当前资源 | 每卡一个本项目训练，最多两项本轮run并行；全项目最多3卡，复用原共享lease |

## 本轮修改与验证

保留已验收的`batched_selection_v1.py`与原独立训练器，未修改分类算子、数据或优化配方。新增的都是执行包装：

| 文件 | 作用 |
|---|---|
| train_c1_budget.py | 接入既有批量化C1；自然epoch边界计时、源码快照与训练回执；每轮一次CUDA同步用于准确墙钟 |
| evaluate_c1_budget.py | 在同一lease物理卡上调用原完整dev评价器，先固定可见CUDA上下文 |
| run_budgeted.py | 统一lease准入，训练结束后评价，资源检查与总墙钟截止；失败/超预算留原产物 |
| budget_policy.py | 不读取AP的耗时外推，以及E200/完整dev端点检查 |
| campaign.py | 有限42→0→123队列，最多两个run并行；首5轮通过后放行后续seed |
| test_budget_execution.py | 已知真值测试：耗时外推、部分端点拒绝、dev覆盖、子进程退出码和真实截止终止 |

94 pinned环境7/7运行控制测试通过；42/0/123三份原配置语义一致、原readiness通过，CPU预检均未初始化CUDA。原分类加速已有32项CPU测试、真实raw对照和106批/100成功更新证据，本轮未重复这些已完成测量。

批量化保留科学设置和样本流，但浮点归约顺序变化可改变连续训练数值轨迹；不能称逐位等价。正式结果比较保留这一工程限制。

## 时间与队列合同

- 从取得GPU开始计时，到末轮保存与独立dev评价完成，总墙钟最多10小时；等待资源单列。
- 前5个自然epoch属于同一个E200进程，覆盖warmup；最近3个完整epoch均值外推剩余轮数，另留1小时尾部预算。预算门只看耗时，不看AP。
- 第5轮通过则seed0可在另一张动态选择的卡启动；本轮最多两个run并行，seed123待空出一个run位置后继续。
- 每个run各有10小时硬截止。若工程预算或资源失败，保留该attempt并暂停尚未启动的seed；不会把不完整结果写成E200，也不据中途AP停止。
- 原全局guard保持原文件；本轮进程内将物理卡数上限设3，整卡至少2GiB余量，项目显存低于70%，项目RSS不超过300GB十进制；加入前按已有实测峰6304MiB/28814MiB预约8192MiB/32768MiB。

16:36快照无空卡；16:46实际准入时GPU0已空出，任务动态选中GPU0。资源以`admission.json`及持续`resources.jsonl`为证，不沿用旧快照。

## 运行与产物

- 94根：`/mnt/dataset/yudongfang/projects/RGBT_campaign/artifacts/c1_budget10h_20260909/`
- 源码：`release_v1/`；测试：`cpu_tests.txt`；预检：`preflight_s{42,0,123}.txt`。
- 队列：`attempt1/campaign_status.json`，tmux `c1budget_queue_42`。
- seed42：`attempt1/C1_seed42/`，tmux `c1budget_paired_42`；本项目训练、评价始终共用原lease。
- 每run：`run_status.json`、`admission.json`、`resources.jsonl`、阶段launch及日志、`run_final.json`。
- 训练：`training/epoch_timing.jsonl`、`budget_progress.json`、原训练/评价回执与checkpoint；权重只保留服务器，不复制到GitHub。
- 本地：本目录源码、`source_inputs/`原入口只读副本，以及后续采集的`evidence/`小产物。

## 当前结论与下一步

当前确认的是新执行链可启动、配置验收通过；真实稳定epoch速度和最终AP仍待本run产生。旧100批计时0.22136秒/批、E200纯训练6.92小时仍属于短窗外推，不能写成已完成耗时。

C1按既有三seed对N/C0的条件判断保留，再执行最终版本的归因。定位与外部作者模型从头复现未在本轮新增训练。阶段证据同步既有GitHub分支`research/full-evidence-20260906`。

## 实际更新核对（2026-09-09T16:52:00.472486+08:00）

seed42已完成90个真实batch、84次成功参数更新，AMP skips=6，当前第1/200轮；最近KD标量=0.36594269。当前资源记录无越界，整卡剩余17758MiB，项目进程树RSS=28820MiB。首5轮耗时门尚未完成，0/123保持待启动；当前日志没有完整AP。详见[evidence/status_20260909_165200.json](evidence/status_20260909_165200.json)。

## 最新核对与自动跟进（2026-09-09T16:55:17.842436+08:00）

seed42第1/200轮，390批、383次成功更新、7次启动阶段AMP skip；首5轮门仍待完成，当前稳定整程速度不能用旧短窗替代。日志提示磁盘读取偏慢，初期实际batch吞吐低于旧短窗，尚不能把差异完全归因于I/O；工程门会阻止不合10小时预算的后续铺开。见[evidence/status_20260909_165517.json](evidence/status_20260909_165517.json)。

原`RGBIR 独立蒸馏实验推进`自动跟进已更新为本轮任务并恢复每小时执行；旧子集/梯度探针排程已替换。它检查实际队列、资源、预算和端点，只在有结果/故障/去留决定时通知；不能重启旧慢版或重复当前run。
