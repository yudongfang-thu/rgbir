# 共享卡预算失败后的原配置重启（2026-09-09 17:55）

**seed42已通过5轮预算门并持续运行；seed0原attempt在第6轮因工程耗时外推超过10小时中止，保留原产物；本次仅将后续准入收紧为启动时空卡，原训练、预算和评价不变。**

原42第2–27轮约124–136秒，第27轮外推30081.31秒（含1小时尾部），约8.36小时。seed0第6轮201.38秒，最近3轮均163.30秒、外推36205.81秒，超过36000预算205.81秒。其所在GPU4启动前已有他人负载，后续显存占用变化；这支持共享负载是排查重点，尚非受控因果实验。没有AP或方法负结果。

保留旧`attempt1/C1_seed0`，不resume。新`release_v2`只修改调度候选为当前整卡占用低于100MiB，复用既有统一lease；3个训练/评价/预算脚本逐字节保留v1。有限新队列继续读取原seed42的真实状态，最多2个run同时存在，依次补seed0新attempt和seed123。原seed42进程和源码不修改。

后续仍按原全量E200、同初始化、λ、loader和10小时规则；新seed0的首5轮也在该E200中验证。未空出合格GPU则30秒排队，不把可用显存等同可用吞吐，不占用现有seed42的卡。

## 新attempt实际执行（2026-09-09T17:58:26.013758+08:00）

seed0新attempt于17:56:58取得当时空闲的物理GPU6，现有283次成功参数更新；原seed42保持GPU0，第30轮。新队列`recovery_attempt2/campaign_status.json`负责剩余0/123，原`attempt1/campaign_status.json`保留失败后暂停状态，不再据原队列误判新0状态。新状态入口为`release_v2/collect_status.py`。

7项运行控制测试及seed0原准入预检再次通过；新的训练/eval/budget内容保持v1，资源候选只用启动时空卡。重启不继承旧6轮checkpoint，不改学习率、数据、目标选择或损失。当前仍无完整AP；seed0新attempt的完整耗时门待5轮产生。

身份复核：三个训练/eval/budget脚本与v1逐字节相同，原配置预检通过。正式训练器未保存initial_student张量，额外的初始张量直接比较不可执行；不据此声称重启后的全部数值轨迹逐位相同。该限制见recovery_identity_receipt.json，未改变训练。
