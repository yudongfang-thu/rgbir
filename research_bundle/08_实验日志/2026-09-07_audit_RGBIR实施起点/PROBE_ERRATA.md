# 定位差距探针勘误建议（原件保留）

> **A桶是阈值和历史模型条件下的对象级定位差距，不是可恢复率或同anchor DFL市场。B桶实际仅学生优，不是双优。**

原件：`08_实验日志/2026-09-07_probe_定位差距分解/{loc_gap_probe.py,README.md,summary.json}`。本次只读，不改代码/summary/计数。

| 原表述 | 实际证据与修正 |
|---|---|
| 夜间16.1%可恢复 | 低亮度代理桶1876/11681，历史S同类IoU∈[.5,.75)、T对RGB GT IoU≥.75；应称对象级差距候选，能否学习、同anchor/DFL与净AP增益待验证 |
| B=双优 | 代码只判`if s_iou >= IOU_GOOD`，不判T；改称“学生定位优”；summary不足以再分B中教师失败数 |
| D无观测/不可蒸 | conf≥.10、同类IoU≥.5匹配丢掉低置信/错类/定位差/raw候选；只能称“该后处理阈值下学生未匹配且教师匹配” |
| E双方困难 | 受阈值、类别和NMS影响，只能称该阈值下双方未匹配，不等同传感器不可见 |
| C teacher no better | T<.75可能仍优于S，例如.74对.50；应称双方未达.75且学生匹配 |
| 框IoU .728证明对应可行 | A桶已由同一个RGB GT建立对应，均值只描述成功选中的对；不证明无GT身份、错配尾部、坐标变换或同anchor语义 |
| 定位市场大于判别市场，上限高 | 分母、模型和条件不同，.9pp历史参照增益不是对象比例；删除跨分母/跨指标比较 |
| 昼夜 | 单图`convert('L')`像素mean与78.283比较，只是亮度代理；源码median指阈值历史出处 |

本probe学生为历史`cgkd_w1/native_rgb_s42_e200`，L计划冻结R却是`formal_native/dronevehicle/rgb_seed42_native_b32a2`，所以A桶不是L冻结R的eligible数。T直接在IR原图坐标对RGB GT计算IoU，未读独立IR GT或审计局部变换；A桶不能证明数据配准。类感知贪心匹配也不同于新D1类无关空间匹配和OEv1 GT Hungarian匹配。

可保留原summary：A高亮度220/10781、低亮度1876/11681，总2096/22462；car占1930/2096；S/T IoU均值.680/.851，框间IoU均值.728。仅描述原始对象差距，不新增可恢复或训练成功结论。

建议root在原README/索引顶部加勘误链接，保留历史正文和脚本，新D1/D2及本条目优先解释。
