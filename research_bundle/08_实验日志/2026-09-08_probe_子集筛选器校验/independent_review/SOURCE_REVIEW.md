**READY_FOR_GPU_PREFLIGHT。** 新子集N/C0源码与部署逐字节身份闭合；10个运行Python/配置/AMP输入已保存独立快照。实际pinned 12项检查（wrapper8+队列时钟/本PID watchdog4）及3个CLI通过，CUDA未初始化；另有独立本地8项、预算7项与分析器6项受影响小检查。无新增旧分类/数学大型矩阵。

原C0公式、门和base沿原pinned superclass保留。新接口只绑定通用预训练五类学生、固定2048子集、完整dev、正常BN、共同初始state及前30批服务器双模态uint8像素/标签逐元素比较。30批只证明前缀；raw张量不上传、不计算hash。预算采用全部完整batch实测与I/O、初始化开销、至少20%训练余量和两次eval，并将45分钟执行时间与排队分列。

准入只允许这份源码经原lease开始真实N/C0 canary；必须实际通过共同state/像素、有限梯度与非零C0信号、资源和预算门后才跑两臂完整512批/E8及独立dev。当前READY不表示GPU canary或最终AP已通过。正向仅保留已知对照方向，非正向停止此筛选器，不否定C0或证明一般排名/E200预测性。

最终预算源相对7真值版仅将误名measured_framework_peak_mib改为measured_combined_peak_mib，数值和算式不变，已逐字节差异核验。精确文件/回执见SOURCE_REVIEW.json。
