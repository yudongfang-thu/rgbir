# 首个E8端点：N／seed42

**N完成独立E8日程，完整dev mAP50–95=43.376542；这是短程原生参照原值，尚无C0/C1匹配比较，不是蒸馏增益或正式论文结果。**

## 设置与完成身份

Drone、seed42、原YOLO11n通用初始化，E8及8轮LR日程、warmup3、640、batch32/nbs64/workers4、原冻结增强和SGD设置；分类/定位附加系数均0。固定第8轮last/EMA，完成4504批（563×8）、2667次成功更新、7次AMP跳步、2674次attempt/EMA计数。EMA计数不能误写成成功optimizer更新数。

原训练总耗时3057.788秒（50.963分钟）。CSV各轮耗时从约531秒降至末四轮271–287秒；共享GPU负载期间发生变化，不能把这个下降归因于代码优化或蒸馏。训练CSV累计3040.510秒与入口总耗时口径不同，不拼成同一种计时。

## 独立评价原值

完整dev为1469图、22462个GT；原pinned native evaluator，固定last/EMA，评估耗时14.542秒。

|指标|百分制数值|
|---|---:|
|mAP50–95|43.376542|
|AP50|64.983495|
|AP75|50.301986|
|Precision|66.995544|
|Recall|62.477873|

|类别|mAP50–95|AP50|AP75|
|---|---:|---:|---:|
|car|61.902406|92.551201|71.738537|
|freight car|22.687248|39.065753|23.178334|
|truck|38.948366|56.944734|45.665486|
|bus|65.162269|90.746482|79.115418|
|van|28.182420|45.609303|31.812153|

原JSON单位为0–1，本表乘100；没有跨seed SD或效应显著性判断。不能与旧E200结果直接相减。C0/C1将依预先固定的同一E8日程与评价条件完成，不按该N结果调整方法或预算。

## 资源与产物

训练实测NVML峰值7630MiB、进程树RSS28957MiB；评估1370/4052MiB，均在已冻结预约内。训练和独立评估均完成原lease阶段；检查原始队列资源回执，不用完成后的空卡状态替代运行中监控。

`runs/N/`保存实际配置、完整训练回执、原CSV和日志等；`evaluations/N/`保存独立评估回执、实际评价合同与dev roster；`queue/`保存完成与资源回执。`collection_receipt.json`列出服务器原路径、stat及是否只留远端；大权重和压缩逐对象预测保留在服务器，未新计算hash。

**日志局限：原训练results.csv前7轮的dev指标为禁用训练内评估的占位；末轮只有8列、少于15列表头，最后三个值是LR，不能按表头读取为AP。** 原CSV保持不改；上表唯一AP来源为 `evaluations/N/short_evaluation_receipt.json`，不是训练CSV。独立回执交叉核对见 `ENDPOINT_REVIEW.md`。
