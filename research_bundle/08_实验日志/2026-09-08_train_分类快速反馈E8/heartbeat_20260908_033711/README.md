# 03:37 只读核对

**N仍在第1/8轮持续更新，C0/C1按原顺序待运行；没有新完成端点、失败或AP结果。七项状态/资源检查通过，不需改变队列。**

进度回执：320批、313次成功optimizer update、7次AMP跳步，运行322.14秒，末次未加权KD为0.78713且有限；N实际系数为0，原进度中的`arm=weight0`是继承的底层名称。相比24更新探针的6次跳步多1次，本次仅记录，不能据一次跳步判断训练失效。

项目6个CUDA任务分布于2/4/5三张物理卡，每卡2个；GPU4全卡余量3134MiB，项目各卡显存均低于70%，项目RSS172725MiB（约168.68GiB），低于300GB。当前快照通过不代替持续guard监控。

原始`progress`、队列/故障是否存在、完整资源lease快照见 `snapshot.json`，判定见 `checks.json`，只读脚本副本为 `collect_heartbeat.py`。未启动新GPU任务、未修改远端、未计算hash；完整epoch尚未完成，不追加整轮吞吐结论或修改7.34小时规划值。

随后03:42补记：N已进入第2/8轮，首轮 `results.csv` 累计时间530.879秒（8.85分钟），包含首次轮内开销，尚不能当稳定后续轮均值；相比24批外推的483.321秒约高9.8%，仍在原20%预算余量内，不改变日程或规划。原CSV见 `N_first_epoch_results.csv`，后续progress见 `N_after_first_epoch_progress.json`。CSV中的dev指标0是禁用训练内评估的占位，**不是AP=0的实验结果**；仍无独立完整端点。
