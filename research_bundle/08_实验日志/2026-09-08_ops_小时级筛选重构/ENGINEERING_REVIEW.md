# 小时级子集与 RGB baseline warm-start：最小工程改动审阅

**可沿现有 trainer 加一个独立 screen 包装入口，不需要重写训练循环。确定阻塞是 E8 身份/人数硬编码、T/R 数据来源比较、以及 native profile 同时绑定训练初始化路径；baseline 分类头会被无条件重置的说法不受已读 pinned 源码支持。** 2026-09-08，loc_stress。只读本地已执行源码副本；未修改旧源码、SSH、GPU、加载权重或计算 hash。本报告是实现边界与待验证集合，不是新训练准入。

1. **只在新 screen 副本解除 E8 专属常量，保留正式校验器。** [screen_common.py:30](../2026-09-08_ops_训练吞吐诊断/short_screen_E8_release/screen_common.py) 的 `validate_config` 固定 epochs=8、expected_train_images=17990、model 文件名 yolo11n.pt、E8 endpoint/arm 集合；`require_admission` 又固定 E8 scope/8轮/12小时预算。新入口需显式字段记录 subset roster、人数、warm-start 身份、共同日程及小时预算，创建新 endpoint/scope/attempt。`train_independent.validate_execution(...,formal=False)` 已允许非 E200，不需改原 `protocol.py` 的正式 E200 门，也不能直接调用原 `train_independent.run()` 冒用正式结束路径。

2. **子集改数据入口，不在 loader 运行后随意截断。** 现有 [legacy build_trainer:139](../2026-09-08_ops_训练吞吐诊断/remote_selected_update24_probe_attempt1/comparison_attempt1/old/sources/0034_train_object_evidence.py) 从 student_data_yaml 构建学生，privileged_data_yaml 的 train 构建教师数据集；[DualLabelRGBIRDataset:108](../2026-09-08_ops_训练吞吐诊断/remote_selected_update24_probe_attempt1/comparison_attempt1/old/sources/0033_paired_rgbir_data.py) 按 resolved RGB→IR 映射检查对应。最小做法是在新目录生成 RGB/IR train 清单与新 YAML，保留完整 dev1469。冻结相同成对子集给所有比较臂，验证无重复、都属于已登记 train、RGB→IR 映射齐全、两套独立 GT 未丢失、names/原图 shape/增强后 shape 相容。训练人数和批数按实际 dataset/loader 读出；不要依赖 `fraction` 的排序隐含行为。全量 IR pool 配合 RGB 子集技术上也能映射，但必须明记它仍加载全量教师标签池，不能误报双方都子集化。

3. **T/R 原训练数据身份要与本次子集 YAML 分开。** [load_frozen:52](../2026-09-08_ops_训练吞吐诊断/remote_selected_update24_probe_attempt1/comparison_attempt1/old/sources/0034_train_object_evidence.py) 会比较 checkpoint 旁 `args.yaml` 的 `data` 字符串与调用传入的 expected_data；直接替换 cfg paths 为子集 YAML 将在 `_setup_train` 加载 T/R 时失败。新 screen 的私有 builder 可只替换这一个 `load_frozen` 绑定：限定批准 teacher/reference 路径，调用原加载器时传其原训练 YAML，再独立校验本次数据集类序/成对映射。另一条最小路是保留原 cfg paths，在新 dataset hook 单独传 train roster。两者择一，不能删除来源检查或改 checkpoint 旁 args。沿 E8 保留输入模型/T/R 的前后 path+bytes+mtime stat；本轮不新增权重 hash。

4. **warm-start 使用同一个固定 RGB baseline 端点作为所有新臂初始化，明确不是 resume。** [engine setup_model:803]（服务器/本地保留，未包含于本阶段发布：../2026-09-08_ops_训练吞吐诊断/runtime_sources/engine_trainer.py） 在 `.pt` 模式取得 checkpoint 模型/YAML，然后交给 get_model；`load_checkpoint` 优先 EMA。保留 `resume=false`，由现有 setup 创建新 optimizer、scaler、EMA、seed/loader 和轮次/step计数，旧 baseline 的 optimizer/RNG/epoch 不继承。不得称原 E200 连续训练，也不能只给 C1 warm-start 而把旧从头训练 N 当匹配对照。新配置应分别记录 parent endpoint、initialization_kind、parent stat 和新日程。

5. **无需先写“防分类头重置”的补丁；先验明实际加载完整性。** 已执行 [DetectionTrainer.get_model:188](../2026-09-08_ops_训练吞吐诊断/remote_short_profile_attempt1/profile_C0/source_copies/0067_train.py) 先构造 DetectionModel，再 `model.load(weights)`；[BaseModel.load:311](../2026-09-07_train_IndependentKD实施/remote_admission_1532/evaluator_profile_attempt2/evaluation_profile_binding/sources/009_tasks.py) 对同 5 类同序不做类行重映射，而用 state_dict 交集加载。未见加载后无条件重置 head。实际风险是 `strict=False` 可静默漏 key/shape，以及错 class names/不同模型架构。最小新初始化核验须覆盖所有期望参数和 buffers，尤其分类/box/DFL head：从所选 baseline payload.float 到首次训练 forward 前的新学生状态应逐项匹配，missing/unexpected/mismatched keys 为空；新 EMA 初始与学生一致，optimizer/scaler/计数是新状态。不要求写大权重快照，保留小型逐组计数/最大差与明确时点即可。若真值核验发现 head 未载入，再做私有局部修复。

6. **完整 dev 的 evaluator 复用纯 helper，但绑定身份必须显式拆开。** [evaluator_profile.configuration_identity:33 / validate_evaluation_profile_binding:132](../2026-09-07_train_IndependentKD实施/remote_admission_1532/evaluator_profile_attempt2/evaluation_profile_binding/sources/001_evaluator_profile.py) 将 cfg.model 和 student_data_yaml 也纳入 accepted binding，且校验 data YAML 字节；即使 dev 不变，换 warm-start/subset YAML 也会被拒绝。新 screen 可建立受审的 evaluation identity projection：原 accepted profile 继续验证原评估代码、完整 dev YAML/roster/kwargs；实际待评权重另绑定新 run 的 last stat 和新训练身份。也可另记新 screen binding，但不能把旧 binding 改写成新训练已通过。保留 [evaluate_short_screen.py](../2026-09-08_ops_训练吞吐诊断/short_screen_E8_release/evaluate_short_screen.py) 的实际 kwargs、1469 图/22462 GT、五类有限 AP、checkpoint 前后 stat 和只读 capture；改 E8 completion/horizon 为新独立身份。不要调用原 E200 run/publish/baseline_identity，不需因缩 train 集就缩 dev 或重写指标算法。

7. **保留已验收 selected-only 的学习图与原门/分母，补最终计数即可。** [candidate_builder:19](../2026-09-08_ops_训练吞吐诊断/short_screen_E8_release/train_short_screen.py) 已支持 `make_api + make_criterion_type` 联合绑定；[selected_only_v1.py:217](../2026-09-08_ops_训练吞吐诊断/performance_candidate/selected_only_v1.py) 已保留原 pool/cast、FP16 finite 否则 fallback、全 GT 背景与全 base 分母，完整统计 detached/no_grad 单列。此轮无需重写池化。新 `runtime_ready` / completion 添加实际 subset roster/count、warm-start 核验引用及最终 thin/full/fallback 计数，防止重现 E8 最后 4 批计数缺失。原 C1 系数 0.09227393550836771 来自原初始化/数据的校准；若保持它，应标为新 screen 固定继承系数，不能宣称已在 warm-start 子集重新校准。若改变系数，须在看新结果前另冻结。

8. **只替换新 wrapper 中的预期人数/日程和来源记录。** [train_short_screen.run:81](../2026-09-08_ops_训练吞吐诊断/short_screen_E8_release/train_short_screen.py) 除配置门外还硬验 8轮、17990/1469，completion 字段也固定8；改为与冻结子集清单和新日程相等，保留原 trainer、失败即退出、独占新输出。现有 `copy_sources` 没有自动收新数据 YAML/roster/mapping，必须把它们显式加入小来源副本；生成器、新 wrapper、candidate、批准副本均入 source manifest。不复制权重，不改旧 runtime 或旧 run。新 train/eval completion 与队列阶段门使用新身份，不能向 E8 目录追加结果。短子集还会改变 warmup 迭代数和梯度累积历程：pinned `_get_warmup_iterations` 为 min(warmup_epochs,epochs−1)×实际批数，需记录实际预算，不能把“同 epoch 数”当作旧 E8/E200 的等更新数。

9. **最小验收集合应复用已有检查，避免再造平台。** CPU：确定性成对子集及 count/重叠/错映射负例、旧身份拒绝、新 admission/输出独占、T/R 来源错误拒绝、warm-start names/key/shape 核验、完整 dev projection/roster/actual kwargs、短日程实际 warmup/accumulate 与 batch 计数。随后只在根批准资源后做一次新配置的有界 canary：实际成功更新计数（可沿现有24次门）、有效 KD 梯度与 N 的零剂量、模型/EMA 有限、T/R 无梯度且不进 optimizer、thin 全批与 fallback 计数、新初始化一致性、双模态 GT/首批增强、实测 NVML/RSS及正常日志 cadence。使用正常频率薄学习，完整诊断另计时，避免 max_steps 自动 sanity 每批 fullstats 遮住生产成本；无需重复旧 block16 或整套大 state 轨迹比较。按新子集实际 batch 数和同负载实测速度给出小时预算，包含准备/评估/排队；保留原资源 lease/300GB 总量与预约约束。仅允许小时级方向筛查，不能从旧 E8/单 seed 直接升级正式增益。

工程结论：优先保留 trainer 和已验收 selected-only，新增小型 screen 配置/来源投影/数据清单包装层。上述未执行的初始化、子集流和新负载 canary 为新入口待验证事项，不把既有 exact24 当作新 warm-start/subset 已通过。
