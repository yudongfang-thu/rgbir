# 收录范围、版本和复现边界

这是用于外部模型独立审查的资料包。复制的AGENTS/工作区规范是历史或运行环境证据；它们不要求审计模型连接服务器、申请凭据、启动训练或修改队列。审计任务以根目录REVIEW_PROMPT.md为准。

## 包含

- 原仓库97文件所承载的2026-09-05审计、历史评估JSON、完整results.csv、FreqMix来源与HNEWA多臂结果，原始数值文件保留。
- research_bundle/中完整本地07研究分析、相关08实验日志及诊断图册，含原始小型结果、逐图/逐目标指标、预测、采样/donor清单、特征能量数组、代码和canary/失败/复核记录。
- 两篇文献调研笔记和PAPER_CATALOG.json。题录来自已有文件名，不代表本次重新核验论文身份或旧文献判断。
- OEv1实际训练实现、测试、协议及关键运行依赖；历史KD/OS-SSL模块；选定的SAR历史审计和交接入口，帮助理解方向变化。
- 发布时94只读补采的小产物和pinned第三方源码切片，见remote_snapshot_20260906中的来源清单、LICENSE及时间戳。

## 有意不收录

原始数据集、模型checkpoint、大型训练张量、第三方论文全文、凭据、私钥、环境缓存和完整历史工程镜像。诊断图中的数据样例属于已有分析图册，并非训练集镜像。NPZ是已导出的特征能量图，不等同于全网络激活。

工作区原入口中指向02汇报材料、04服务器环境、05大结果仓、06只读历史工程的部分路径只是来源线索；没有这些大文件不会把其缺失变成“已复核”。无法在本包解析的历史Markdown链接会显示原路径并标明未收录。可复算的结果以实际存在的JSON/CSV及其协议为准。

## 复制与适配

本地source不修改。代码、JSON、CSV、NPZ和诊断图按原字节复制；只对导出Markdown的链接目标做GitHub相对路径适配。BUNDLE_MANIFEST.json记录来源路径、类别、字节数和文档适配。远端大manifest若压缩或去重，来源映射和无损还原信息由remote快照清单记录；日志尾部会明确标注为片段，不能冒充完整日志。

根README已更新。旧入口原文保存在ARCHIVE_README_v1.md，旧问题保留在07_questions_for_auditor.md。更新入口不等于抹去旧判断；阅读旧材料必须同时阅读后来的勘误和MODEL_REVIEW_GUIDE.md。

## 运行与指标

保留的脚本含94绝对路径、Linux fcntl/proc与固定依赖。审查源码、复算导出JSON/CSV可在无GPU环境进行；完整训练/推理仍需合法取得数据、模型、相容CUDA环境并适配路径。本包没有声明已验证跨机器一键复现。

OEv1中间val关闭，训练CSV的AP=0是占位。只有完整E200 last/EMA、实际eval JSON及COMPLETED receipt闭合的端点才能用于性能对比。三student seed使用固定teacher/reference seed42，并在已测范围共享原生数据流，主要覆盖初始化变化；P/N三seed不替代shuffled、same-modal或同剂量归因。

第三方运行时源码保留其原版权/许可，见补采LICENSE；本仓库未给整个混合材料包追加统一软件许可证，也没有把第三方内容重新许可为项目自有代码。
